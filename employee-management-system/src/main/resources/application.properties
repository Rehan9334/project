# ===============================
# Project & Server
# ===============================
spring.application.name=employee-management-system
server.port=8089

# ===============================
# MySQL Database Configuration
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/ems?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=qaisar786
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# Spring Security
# ===============================
# ❌ REMOVE these (don’t set them empty)
# spring.security.user.name=
# spring.security.user.password=

# ✅ Let your custom UserService (DB-based) handle authentication
