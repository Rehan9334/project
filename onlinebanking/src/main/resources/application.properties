# =========================
# Application Info
# =========================
spring.application.name=onlinebanking

# =========================
# Server Configuration
# =========================
server.port=8089

# =========================
# MySQL Database Configuration
# =========================
spring.datasource.url=jdbc:mysql://localhost:3306/onlinebankingdb?useSSL=false&allowPublicKeyRetrieval=true

spring.datasource.username=root
spring.datasource.password=qaisar786
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================
# JPA / Hibernate
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =========================
# JWT Configuration
# =========================
# Secret key used for signing JWT (must be at least 32 characters)
jwt.secret=MySuperSecretKeyForJwtTokenGeneration123456789
# Expiration time in milliseconds (1 day)
jwt.expiration-ms=86400000

# =========================
# Logging
# =========================
logging.level.org.springframework=INFO
logging.level.com.example.onlinebanking=DEBUG
